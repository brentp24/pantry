<h1>{{title}}</h1>

{{#each recipes}}
<div>
    <a href="/recipe/{{id}}">{{name}}</a>
    <ol>
        {{#each ingredients}}
        <li>{{this}}</li>
        {{/each}}
    </ol>
</div>
<hr>
{{/each}}